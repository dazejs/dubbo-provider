

export const enum Constants {
  /**
   * default buffer size is 8k.
   */
  DEFAULT_BUFFER_SIZE = 8 * 1024,
  MAX_BUFFER_SIZE = 16 * 1024,
  MIN_BUFFER_SIZE = 1 * 1024,

  /**
   * 8M
   */
  DEFAULT_PAYLOAD = 8 * 1024 * 1024,
}