

export class Constants {
  /**
   * default buffer size is 8k.
   */
  static DEFAULT_BUFFER_SIZE = 8 * 1024
  static MAX_BUFFER_SIZE = 16 * 1024
  static MIN_BUFFER_SIZE = 1 * 1024

  /**
   * 8M
   */
  static DEFAULT_PAYLOAD = 8 * 1024 * 1024

  /**
   * HEARTBEAT
   */
  static HEARTBEAT_EVENT = null
}